#include "tokenizer.h"
#include "history.h"
#include <stdio.h>

char main() {

  char token;

  printf("$ \n");
  scanf("%d", token);
  //s_List list = init_history();
  /* if (token == "history") {
    print_history(list);
  }
  if (token == "!", a) {
    *get_history(list, a);
  }
  printf("Token: %d", token);
  add_history(list, token);

  **tokenize(token); */
  

 done:
  return 0;
}
